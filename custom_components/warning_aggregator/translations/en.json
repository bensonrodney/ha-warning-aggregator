{
  "title": "Warning Aggregator",
  "config": {
    "step": {
      "user": {
        "title": "Warning Aggregator",
        "description": "What do you want to add?",
        "menu_options": {
          "monitored_entity": "Monitored entity — watch one entity and flag when it goes bad",
          "template": "Template check — a Jinja template decides good / not good",
          "aggregator": "Aggregator — roll a set of labelled entities into one warning"
        }
      },
      "monitored_entity": {
        "title": "Monitored entity",
        "description": "Pick the entity to watch. The next step adapts to whether it is a switch/toggle, a number or text.",
        "data": {
          "entity_id": "Entity to watch",
          "name": "Name",
          "labels": "Labels"
        },
        "data_description": {
          "name": "Defaults to the entity's name with \" status\" appended.",
          "labels": "Applied to the new sensor so an Aggregator can pick it up."
        }
      },
      "configure_check": {
        "title": "When is {entity} a problem?",
        "description": "Detected as a {kind} value (currently: {value}).",
        "data": {
          "bad_state": "Problem when the state is",
          "match_text": "Text to match",
          "comparison": "Comparison",
          "match_mode": "A match means",
          "unavailable_is": "When there is no value (unavailable / unknown / null)"
        },
        "data_description": {
          "match_text": "Compared case-insensitively."
        }
      },
      "numeric_kind": {
        "title": "Number monitor",
        "description": "{entity} is a number (currently {value}). Pick one — a single threshold, or a range.",
        "menu_options": {
          "numeric_threshold": "A threshold — a problem below (or above) one value",
          "numeric_range": "A range — a problem outside (or inside) a low–high band"
        }
      },
      "numeric_threshold": {
        "title": "{entity}: threshold",
        "description": "Currently {value}.",
        "data": {
          "direction": "Problem when the value is",
          "threshold": "Threshold",
          "hysteresis": "Hysteresis (deadband)",
          "unavailable_is": "When there is no value (unavailable / unknown / null)"
        },
        "data_description": {
          "hysteresis": "Optional. The value must move this far past the threshold to flip the verdict, to stop flapping."
        }
      },
      "numeric_range": {
        "title": "{entity}: range",
        "description": "Currently {value}. The bounds can be entered in either order.",
        "data": {
          "direction": "Problem when the value is",
          "range_low": "Lower bound",
          "range_high": "Upper bound",
          "hysteresis": "Hysteresis (deadband)",
          "unavailable_is": "When there is no value (unavailable / unknown / null)"
        },
        "data_description": {
          "hysteresis": "Optional. The value must move this far past a bound to flip the verdict, to stop flapping."
        }
      },
      "template": {
        "title": "Template check",
        "description": "The check is a Jinja template. It's a problem when the template renders truthy (true / \"on\" / a non-empty string); OK when it renders false / \"\" / \"off\".",
        "data": {
          "name": "Name",
          "labels": "Labels",
          "value_template": "Problem template",
          "reason_template": "Reason template (optional)",
          "unavailable_is": "When the template errors or renders nothing"
        },
        "data_description": {
          "labels": "Applied to the new sensor so an Aggregator can pick it up.",
          "reason_template": "Rendered into the sensor's `reason` attribute and the card's list."
        }
      },
      "aggregator": {
        "title": "Aggregator",
        "description": "One problem sensor that turns on whenever a labelled entity is in a problem state.",
        "data": {
          "name": "Name",
          "labels": "Labels to watch",
          "match": "Label matching",
          "problem_states": "States treated as a problem"
        },
        "data_description": {
          "labels": "Every entity carrying one of these labels is watched.",
          "match": "\"any\" watches entities in any selected label; \"all\" only watches entities carrying every selected label.",
          "problem_states": "Turns on when a watched entity's state is one of these. Monitored-entity sensors (device class \"problem\") are always included."
        }
      }
    },
    "error": {
      "name_required": "A name is required.",
      "labels_required": "Select at least one label to watch.",
      "range_equal": "The lower and upper bounds must be different."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Edit aggregator",
        "data": {
          "labels": "Labels to watch",
          "match": "Label matching",
          "problem_states": "States treated as a problem"
        }
      },
      "retune": {
        "title": "Edit monitored entity",
        "description": "{entity} — {kind} check (currently: {value}).",
        "data": {
          "bad_state": "Problem when the state is",
          "match_text": "Text to match",
          "comparison": "Comparison",
          "match_mode": "A match means",
          "value_template": "Problem template",
          "reason_template": "Reason template (optional)",
          "unavailable_is": "When there is no value (unavailable / unknown / null / template error)"
        }
      },
      "retune_numeric_kind": {
        "title": "Edit monitored entity",
        "description": "{entity} (currently {value}). It's set as {current_mode} — pick that one to adjust its values, or the other to switch type."
      },
      "retune_threshold": {
        "title": "Edit monitored entity",
        "description": "{entity} — threshold check (currently {value}).",
        "data": {
          "direction": "Problem when the value is",
          "threshold": "Threshold",
          "hysteresis": "Hysteresis (deadband)",
          "unavailable_is": "When there is no value (unavailable / unknown / null)"
        }
      },
      "retune_range": {
        "title": "Edit monitored entity",
        "description": "{entity} — range check (currently {value}).",
        "data": {
          "direction": "Problem when the value is",
          "range_low": "Lower bound",
          "range_high": "Upper bound",
          "hysteresis": "Hysteresis (deadband)",
          "unavailable_is": "When there is no value (unavailable / unknown / null)"
        }
      }
    },
    "error": {
      "labels_required": "Select at least one label to watch.",
      "range_equal": "The lower and upper bounds must be different."
    }
  },
  "selector": {
    "match": {
      "options": {
        "any": "Any label (union)",
        "all": "All labels (intersection)"
      }
    },
    "bad_state": {
      "options": {
        "on": "on",
        "off": "off"
      }
    },
    "direction": {
      "options": {
        "below": "below the threshold",
        "above": "above the threshold",
        "outside": "outside the range",
        "inside": "inside the range"
      }
    },
    "comparison": {
      "options": {
        "equals": "equals the text",
        "contains": "contains the text"
      }
    },
    "match_mode": {
      "options": {
        "match_is_bad": "a problem",
        "match_is_good": "OK (anything else is a problem)"
      }
    },
    "unavailable_is": {
      "options": {
        "problem": "treat as a problem",
        "ok": "treat as OK"
      }
    }
  },
  "entity": {
    "sensor": {
      "problem_count": {
        "name": "Problem count"
      }
    }
  }
}
