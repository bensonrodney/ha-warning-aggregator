# Screenshots

Referenced by the root `README.md` "Usage" section. Capture from a real Home
Assistant, crop tight, PNG.

| File | What to show |
| --- | --- |
| `create-aggregator.png` | The **➕ Create Helper → Warning Aggregator** menu (both options visible) beside the filled-in **Aggregator** form. |
| `monitor-entity.png` | The **Monitored entity** entity picker beside the adaptive check form for a numeric entity (Threshold / below-above / Hysteresis / "When there is no value"). |
| `card-ok.png` | The card in the all-OK state ("All Sensors OK", green). |
| `card-problem.png` | The card tripped — warning header with a count and 2–3 listed monitors. |

`generate_logo.py` output (the integration/brand artwork) lives one level up in
`brands/` and in `custom_components/warning_aggregator/brand/`, not here.
